# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
build/
dist/

# Virtual environments
.venv/
venv/
# ADR-056: venv aislado de red-team (garak); nunca en deps de runtime
.venv-redteam/
env/

# Test / cache
.pytest_cache/
pytest-of-ronin/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets / local config
.env
.env.local
*.key
*.pem
secrets/

# MCP server config — real file local (puede listar servers/paths privados);
# la plantilla mcp_servers.example.json sí se commitea. Secretos van por
# env_passthrough, nunca en el JSON (ADR-035).
mcp_servers.json

# Atlas runtime artifacts
logs/
*.log
*.out
merkle_log*.jsonl
memory/audit/
.atlas.*
.atlas-audit-home/
docs/audit_complete_latest.json
docs/reality_latest.json
docs/reality_after_24h.json
docs/security_audit_latest.json
docs/self_audit_latest.json
docs/self_audit_*.md
.atlas_self_audit_stop
patches/

# Scratch / brainstorm dumps (raw editor captures, preserved on disk only).
# Their distilled value already lives in docs/membrana/OSM-* and the ADRs.
/idea.md
/idea avance*.md
/gpt.md
/AUDITORIA GROK.md
docs/texto arrastrado.txt
docs/conversation_summary.md
# Raw exported chat — valuable regulatory content; distill into a clean doc before tracking.
/Gemini-Auditoría*.md
# Local agent/skill bundles copied into the workspace; use installed skills instead.
/.agents/
# Stale doc: documents Antivirus/Membrane modules that don't exist in src/ (wrong imports).
docs/security/README.md

# LaTeX build artifacts
docs/*.aux
docs/*.bbl
docs/*.blg
docs/*.toc
docs/*.out
docs/*.log
.autobuild/
.aider*

# LaTeX build cruft (paper) — no versionar; regenerables al compilar
*.aux
*.blg
*.log
*.out
*.toc
*.fls
*.fdb_latexmk

# Artefactos locales de `npx skills` (la fuente de verdad es el catálogo)
.claude/skills/
.agents/skills/
skills-lock.json
