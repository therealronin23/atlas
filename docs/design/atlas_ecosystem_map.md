# Atlas Ecosystem Map

CI ejecuta los tests en un entorno `uv` aislado; la raíz del repositorio forma
parte explícita de `PYTHONPATH` y los módulos auxiliares son paquetes
importables, evitando depender del directorio incidental del runner.

Canonical map for Atlas state after the 2026-07-07 context recovery. This file
classifies the ecosystem; it is not a feature roadmap. The strategy is selective
assimilation without cloning: study external systems, absorb useful capabilities
or patterns, wrap them with Atlas invariants, measure, and improve.

## Definitive Candidate interpretation (2026-07-27)

Este mapa es autoridad de **taxonomía y relaciones**, no de granularidad
operativa. `ACTIVO` aquí significa línea vigente, no `LIVE_VERIFIED`. El estado
actual, sus evidencias y el siguiente paso viven en `STATUS.md` y
`docs/canon/component_reality_matrix.jsonl`; en conflicto, esas proyecciones
frescas prevalecen para realidad. Las cifras y smokes fechados de filas antiguas
son evidencia histórica.

## Taxonomy and States

Taxonomy: `Core`, `Capability`, `Adapter`, `MCP Surface`, `Skill`, `Prompt`,
`Knowledge Source`, `Absorbed Pattern`, `External Service`, `Governance`,
`Memory/Lesson`.

States: `SELLADO`, `ACTIVO`, `PENDIENTE`, `PARK`, `VAPOR`, `MURO`.

## Canonical Map

| Item | Taxonomy | State | Evidence | Authority | Relationship to Atlas | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Gates A-I historical foundations | Governance | SELLADO | ROADMAP closed foundations; gate closure docs | `ROADMAP.md`, `docs/decisions/gates/CLOSURE.md` | Historical runtime foundation | Keep as sealed reference; do not duplicate state |
| ADR-024..040 runtime/governance base | Governance/Core | SELLADO | ROADMAP closed foundations; scheduler ColdUpdate stays propose+validate by default (`ATLAS_COLD_UPDATE_AUTO_APPLY=0`), with application requiring explicit opt-in/HITL. Project-graph rebuild releases native Kuzu query results before the next loader opens the same staged DB. | `docs/decisions/adr/`, `ROADMAP.md`, `src/atlas/core/orchestrator_parts/maintenance_facade.py`, `src/atlas/core/graphs.py` | Observability, ColdUpdate, MCP client, untrusted boundary, decider | Keep sealed unless a new ADR explicitly supersedes |
| BwrapJail y contención de comandos | Capability/Governance | ACTIVO | Ejecución Python y argv estructurado pasan por namespaces/mount/net bwrap, cwd read-only por defecto y fail-closed sin binario; pruebas reales cubren patch/git. Seccomp actual es denylist x86_64, no la allowlist final | `src/atlas/security/bwrap_jail.py`, `src/atlas/security/sandbox.py`, `src/atlas/security/executor.py`, ADR-055 | Límite kernel para ejecución no confiable y comandos autorizados | Mantener; diseñar/decidir la allowlist seccomp portable antes de declarar Slice 2 cerrada |
| Reality kernel | Core | ACTIVO | `atlas reality` command; browser readiness now checks Playwright's expected executable | `src/atlas/core/reality.py`, `ROADMAP.md` | Source of truth for live state | Keep checks honest; avoid provider/live claims without smoke evidence |
| Selective assimilation strategy | Governance | ACTIVO | User preference recorded as memory | `feedback-absorb-without-cloning.md`, `MEMORY.md` | Guides external repo/tool absorption | Apply before adding any external capability |
| Absorption master plan | Governance/Absorbed Pattern | ACTIVO | Hermes/Codex/Cursor/Claude/Crawl4AI/Stirling/desktop sections | `docs/design/absorption_master_plan.md` | Working method for studying and wrapping external systems | Continue as detail doc, not live status |
| MCP trunk | MCP Surface | ACTIVO | `.cursor/mcp.json` wires atlas-trunk; manifest synced; Codex stdio smoke verified `tools/list`, `trunk_kinds`, `trunk_recommend_stack`, `trunk_prepare`, `trunk_health`; adopted configs are deduplicated behind curated entries and preserve per-server timeout | `docs/design/mcp_trunk_portable.md`, `.cursor/mcp.json`, `src/atlas/mcp/trunk_server.py` | Interoperability surface and task preflight surface for Cursor/clients | Keep manifest parity test; reload Cursor after config change; require explicit adoption before executing third parties |
| Project knowledge graphs | Capability/Knowledge Source | ACTIVO | Kuzu/AST is the structural authority tied to Git SHA; Graphify/GraphRAG is a semantic hypothesis layer. The strict semantic route performs verified per-source checkpoints before atomic full-scan publication and exports Neo4j/Obsidian artifacts; vault ingestion evolves the `cohesion` column additively | `scripts/graphify_semantic_resume.py`, `scripts/run-graphify-quality-pipeline.sh`, `scripts/update-knowledge-graph-rag.sh`, `src/atlas/memory/obsidian_to_kuzu.py`, `graphify-out/`, `src/atlas/graph/` | Low-token structural impact analysis plus richer semantic discovery without treating inferred edges as facts | Keep Kuzu/MCP fresh on every published commit; run semantic strict deliberately, resume only misses, and confirm semantic findings against structure/code/tests |
| Cursor MCP wiring | MCP Surface | ACTIVO | `.cursor/mcp.json` committed; atlas-trunk stdio to `~/atlas-mcp` + repo root; env includes `src` + venv `site-packages` for sanitized clients | `.cursor/mcp.json` | Cross-play for Cursor agent sessions | Verify connection in Settings → Tools & MCP after reload |
| MCP 2026 shortlist | MCP Surface/Governance | ACTIVO | `trunk_recommend_stack` ranks installed/verified first; `trunk_health` surfaces configured servers, missing env names, read-only tools, and only curated `research-2026` trial candidates (never the raw historical bulk) | `docs/design/mcp_catalog.yaml`, `src/atlas/mcp/catalog.py`, `src/atlas/mcp/trunk_server.py` | Curated discovery without blind installation | Run trial/security review/consent before promoting any candidate |
| Discovery quality gate | MCP Surface/Governance | ACTIVO | `run_quality_gate` is fail-closed on LLM error and requires `REAL`, `RELEVANTE`, and `HUECO` verdicts before a discovered item can be appended as a candidate. `curated_sources.yaml` adds bounded primary-source material through exact publisher domains plus SSRFBridge; `official` findings are research only, never an adoption bypass. The daily research tick additionally keys its state to the manifest SHA-256, so an approved catalogue change is processed once without resetting history; research, ingestion, graph and MCP phases run before dep/self-build, the slow auto-audit batch, and MCP analysis that can stall on a provider. | `src/atlas/core/self_maintenance/mcp_discovery_quality_gate.py`, `src/atlas/core/self_maintenance/curated_sources.py`, `src/atlas/core/orchestrator_parts/maintenance_facade.py`, `src/atlas/core/self_maintenance/scheduler.py`, `docs/knowledge/curated_sources.yaml` | Prevents broad ecosystem findings from becoming an adoption shortlist merely by volume or topical similarity | Run fresh discovery/ingestion; verify source, license, supply-chain/trial evidence, then require explicit consent for adoption |
| Supply-chain admission scan (A1) | Capability/Governance | ACTIVO | ADR-072; bounded metadata-only scanner, strict Pydantic/JSON Schema report and focused regression coverage | `src/atlas/security/supply_chain.py`, `schemas/supply_chain_report.schema.json`, `docs/design/supply_chain_admission_scan.md` | Local evidence consumed by declarative plugin admission and the A3 materializer/activator; grants no execution authority by itself | Keep; executable plugin types require a new ADR |
| ShadowRouter/DriftTripwire + immune learning | Capability/Governance | ACTIVO | ADR-074; `CODE_PRESENT/TESTED/WIRED`; `ATLAS_PIPELINE_GATE_D=1` fue observado en el proceso baseline, por tanto `RUNTIME_CONFIGURED`, no tráfico live | `src/atlas/security/shadow_model.py`, `src/atlas/security/drift.py`, `src/atlas/transparency/gateway.py`, `src/atlas/core/inference_hub.py`, `src/atlas/core/orchestrator.py`, `src/atlas/immunity/live_loop.py`, `docs/decisions/adr/adr_074_shadow_router_drift_wiring.md` | Slice acotado de defensa activa detrás de Gate D; no promueve Membrane/Osmosis completo ni demuestra eficacia productiva | Mantener umbral/judge fail-closed y capturar evidencia fresca antes de `LIVE_VERIFIED` |
| Declarative PluginManifest v1 + A3 materializer/receipt/activator | Plugin/Governance | ACTIVO | ADR-073; strict manifest, staging containment, supply-chain binding; A3.1 materializer (tree-hash provenance, symlink apply), A3.2 Merkle receipt + HITL via the existing Decider, A3.3 reversible activator — closed 2026-07-22 | `src/atlas/mcp/plugin_manifest.py`, `src/atlas/mcp/plugin_admission.py`, `src/atlas/mcp/plugin_materializer.py`, `src/atlas/mcp/plugin_receipt_broker.py`, `src/atlas/mcp/plugin_activator.py`, `docs/design/plugin_manifest_v1.md` | Full local-source path A closed end-to-end: staged plugin -> receipt -> activation -> revocation, `SkillStore` discovers activated skills | Remote sources and executable plugin types require a new ADR; keep local-only invariant until then |
| Trunk task preflight | MCP Surface/Absorbed Pattern | ACTIVO | `trunk_prepare(goal)` returns compact task packets from catalog/workbench/usage signals; AtlasCoder/ToolCoder inject a fail-open preflight section before coding loops | `src/atlas/mcp/trunk_prepare.py`, `src/atlas/core/trunk_preflight.py`, `tests/test_trunk_prepare.py` | Makes the trunk a context control plane per task without Redis/LangGraph dependencies | Measure real tool usage and refine catalog purposes/aliases |
| Playwright MCP | MCP Surface/Capability | ACTIVO | El preflight fresco demuestra browser/Chromium ready; el handshake MCP citado es histórico y los dos MCP configurados no quedaron live-verificados | `docs/design/absorption_master_plan.md`, `STATUS.md` | Browser-accessibility external surface, separada del estado MCP | Repetir handshake fechado antes de afirmar MCP live |
| Crawl4AI | Capability/Knowledge Source | ACTIVO | Absorption doc: isolated venv, SSRF, untrusted wrapping, tests | `docs/design/absorption_master_plan.md` | Scraping/knowledge ingestion capability | Keep; revisit benchmark vs agent-browser only when needed |
| Stirling PDF | External Service/Capability | ACTIVO | Absorption doc: deployed via Docker with wrapper route | `docs/design/absorption_master_plan.md` | PDF capability through governed external service | Keep as absorbed service; do not fold into core |
| Desktop-control | Capability/Adapter | ACTIVO | `DesktopAction`/`DesktopTool`/`DesktopPlanner` fully wired 2026-07-24: `"desktop"` route (observe/windows/click/type/key/move/**plan**) live in `gate_f_parser.py`/`gate_f_executor.py`, `PolicyEngine` really evaluates `pol_hard_computer_use`, `GateFExecutor.get_desktop_planner()` lazily builds a real `DesktopPlanner` from the Orchestrator's `InferenceHub`. E2E acceptance closed: `test_desktop_plan_executes_across_two_different_real_apps` runs the planner (fake deterministic `InferenceHub`, zero real LLM) but executes the resulting plan for real against Xvfb `:99` + `fluxbox` + `computer-control-mcp`, same `DesktopTool` as click/type/key, zero app-name-conditioned branching. `scroll`/`drag` kinds are honestly reported as unsupported (no MCP tool for scroll; `DesktopAction` doesn't carry two coordinate pairs for drag) rather than faked | `src/atlas/tools/computer_use/`, `tests/acceptance/test_t3_1_desktop_operator_e2e.py`, `~/.config/systemd/user/atlas-xvfb.service` (Xvfb+fluxbox), `docs/backlog.yaml` (`t3-1-universal-gui-operator`, done) | Real host-control capability, gated by HITL (Gate F approval) + PolicyEngine | Real scroll/drag execution and `t3-2-gui-recipe-library` (reusable action recipes) remain open follow-ups |
| Hermes Agent | Absorbed Pattern/External Service | PENDIENTE | Código y despliegue endurecidos para Hermes oficial `0.18.2` fijado a commit; skill firmada y adapter kanban probados en aislamiento. Evidencia actual 2026-07-16: `atlas reality` informa `hermes.mode=mock`, `configured=false`, `live_verified=false`; no hay prueba viva actual de VPS, proveedor ni Telegram | `scripts/install_hermes_agent_vps.sh`, `scripts/deploy_hermes_vps_oneshot.sh`, `scripts/hermes_skill_atlas_twin/`, `scripts/verify_twin_pairing.sh`, `src/atlas/hermes/kanban_bridge.py`, `src/atlas/hermes/hermes.py`, ADR-026..029 | Fuente de patrones y peer opcional, siempre detrás de invariantes Atlas | Desplegar solo con decisión operativa; después exigir por separado pairing firmado, inferencia real y entrega Telegram antes de cambiar a ACTIVO |
| Codex CLI / Cursor / Claude Code | Absorbed Pattern | ACTIVO | Cross-comparison found concrete coding workflow gaps; portable project hook configs share the operating loop and capability router without committing machine credentials; commands are tested from a subdirectory, but live Codex loading remains subject to project-layer trust | `.claude/settings.json`, `.codex/hooks.json`, `scripts/capability_route_hook.sh`, `tests/test_agent_workspace_hygiene.py`, `docs/design/absorption_master_plan.md` | Sources for patching, repo context, permissions, hooks | Review/trust the changed project hooks in Codex; keep `.codex/config.toml` local/ignored and credentials outside Git |
| Atlas shell toolchain | Capability/Adapter | ACTIVO | Vite 7.3.6 + React 18 build on pinned Node 22.22.2; current lock audits clean; CI performs exact install, npm audit and production build | `ui/atlas-shell/package.json`, `ui/atlas-shell/.node-version`, `.github/workflows/ci.yml`, ADR-059 | Reproducible validation harness for the local Atlas OS bridge, not the final product UI | Keep dependency pins and CI gate; revisit Tauri separately |
| MemGPT / MemPalace class systems | Absorbed Pattern/Memory/Lesson | PENDIENTE | User strategy; not yet reconciled to code evidence in this pass | `MEMORY.md` | Memory architecture inspiration | Audit concrete mechanisms before implementation |
| `.claude/skills/` local bundle | Skill | SELLADO | `git rm --cached -r .claude/skills`; local files still present; `.gitignore` covers it | `.gitignore`, context audit | Install/cache artifact for external agents | Keep out of Git; reinstall/update through skill tooling |
| Skills as Atlas ecosystem objects | Skill | ACTIVO | Trunk design: skills are "saber", MCP is "hacer" | `docs/design/mcp_trunk_portable.md` | Procedural knowledge, separate from MCP surface | Serve/install with provenance and consent, not as repo dump |
| Prompts/franken-prompt modules | Prompt | MURO | MCP design: MCP cannot impose system prompt | `docs/design/mcp_trunk_portable.md` | Advisory templates unless delivered by real client channel | Do not overclaim; only implement through real wrapper/hook |
| Knowledge-src root | Knowledge Source | ACTIVO | `KnowledgeTrunk` ingests API sources with provenance; `knowledge-src/preferencias` is classified as policy/design seed | `src/atlas/mcp/knowledge_trunk.py`, `src/atlas/mcp/memory_trunk.py`, `knowledge-src/preferencias` | API/source ingestion into Atlas memory plus explicit factual/personal memory routing | Keep raw source data provenance-backed; do not ingest policy notes as facts |
| Dependency floors | Governance | SELLADO | `pyyaml>=6.0.3`, `cryptography>=49.0.0`, and `litellm>=1.89.0` accepted; tests/mypy/reality pass; `uv lock --check` blocked by redteam resolver conflict | `pyproject.toml`, `WORK_LEDGER.md` | Existing dependency floor declarations, not new dependencies | Keep floors unless a dependency audit requires newer ones |
| Compliance gateway/Osmosis | Capability/Governance | PENDIENTE | P12 es programa permanente de research, Membrane y auditoría bilateral; ADR-051 y varios OSM siguen propuestos, ADR-074 solo promueve un slice | `docs/decisions/adr/adr_051*`, `docs/membrana/`, `PROGRAMS.md` | Línea estratégica viva sin enforcement/producto general aceptado | Operador debe elegir guarantee/enforcement profile; no aparcar ni sobreafirmar |
| Universal verifier ADR-041 | Core | PENDIENTE | ROADMAP build-up layer, core present but wiring deferred | `ROADMAP.md`, ADR-041 | Evidence gate for artifacts | Wire only when it closes a current capability path |
| Cascade routing ADR-042 | Core | PENDIENTE | ROADMAP/backlog says operational wiring deferred | `ROADMAP.md`, `docs/design/backlog.md` | Producer routing by cost/evidence | Keep pending; do not declare live |
| Swarm / worker backend ADR-045..046 | Core | PENDIENTE (scheduling) / ACTIVO (substrate) | ROADMAP says autonomous scheduling gated/deferred; but ADR-046's own text is `Estado: aceptado (núcleo)` — `WorktreeWorker`/`swarm_backend.py` substrate IS accepted+used (worktree-per-task pattern reused by ColdUpdate/self-build daemon), only the AUTONOMOUS scheduling layer on top remains gated | `src/atlas/core/swarm_backend.py`, `ROADMAP.md`, `docs/design/backlog.md` | Substrate: governed diff production via isolated worktrees. Scheduling: still off | Keep scheduling off until objective autonomy criteria exist; substrate itself is not vapor |
| LessonStore ADR-044 | Memory/Lesson | ACTIVO | `workspace/lessons/` is the shared authority for Atlas self-maintenance wiring and twin-local learning continuity | `src/atlas/core/orchestrator_parts/maintenance_facade.py`, `src/atlas/mcp/trunk_server.py`, `WORK_LEDGER.md` | Verified lessons and avoid patterns | Keep one shared store; do not fork Atlas/Hermes lesson state |
| Browser/computer-use local runtime | Capability | ACTIVO | Playwright Chromium/headless shell v1223 installed; `pytest -m "computer_use"` passes | `src/atlas/tools/browser.py`, `src/atlas/core/reality.py` | Host browser control | Re-run browser checks after Playwright upgrades or cache cleanup |
| Zero-importer module radar | Governance | ACTIVO | `scripts/sanitation_audit.py` reports no unclassified vapor and lists 15 classified zero-importer modules | `scripts/sanitation_audit.py`, `docs/audits/audit_context_premortem_2026-07-07.md` | Keeps unwired/entrypoint/parked capabilities honest without deleting tested components | Keep classifications current; unclassified vapor must be wired, parked, or quarantined |
| Computer-use / InferenceHub auto-mode / Statistical validation (early runtime baseline) | Core | SELLADO | ADR-013b (resolved, Gate F — extended by the desktop-control work closed 2026-07-24); ADR-016 (InferenceHub auto/live/stub modes); ADR-019 (statistical validation v1, sealed 2026-05-25) | `src/atlas/tools/computer_use/desktop_tool.py`, `src/atlas/core/inference_hub.py`, `src/atlas/lab/evaluator.py` | Early runtime decisions, all closed | Historical baseline; no action needed |
| Verification/security/memory core cycle (2026-06) | Core/Governance | ACTIVO | ADR-043 (verifiable authorization, Fase 0-1 aceptado); ADR-048 (VerifiedProducer, núcleo completo); ADR-049 (knowledge organism, slice 1 aceptado); ADR-053 (gateway trust completeness, núcleo aceptado); ADR-056 (red-team tooling Garak+PyRIT, aceptado enmendado 2026-07-15); ADR-057 (memory canonical by use-case, aceptado) | `docs/decisions/adr/` (uno por número) | Núcleo de gobernanza/verificación cerrado en junio 2026 | Bundle histórico; revisar individualmente solo si se reabre alguno |
| Verificación adversarial / Odysseus / antivirus inmune para IA — programas propuestos | Core/Security | PENDIENTE | ADR-047/052/054 siguen propuestos; ADR-074 aporta un slice `CODE_PRESENT/TESTED/WIRED/RUNTIME_CONFIGURED`, no producción verificada | `docs/decisions/adr/adr_047_adversarial_verification_and_grounding.md`, `adr_052_odysseus_full_assimilation.md`, `adr_054_defense_in_depth_deception.md` | La tesis amplia conserva autoridad propuesta; un slice no cierra el programa | No declarar ADR-054 cerrado ni live por ADR-074 |
| Integration Fabric / PolicyEngine convergence / Gate Engine | Core/Governance | ACTIVO | ADR-060 (Integration Fabric + Easy Connection + PolicyEngine, aceptado 2026-07-10); ADR-062 (convergencia PolicyEngine↔/permissions/evaluate, aceptado 2026-07-11); ADR-063 (Gate Engine real, aceptado 2026-07-11) | `src/atlas/fabric/policy.py`, `src/atlas/fabric/concierge.py`, `src/atlas/api/product_routes.py` | Capa de gobernanza activa de conexión/permiso/gate | Keep; extender solo con nuevo ADR |
| Ciclo continuo de vetting de MCPs remotos | Capability/Governance | ACTIVO parcial | ADR-075 + ADR-076 A/B: stage1 y partes acotadas de stage2A/2B tienen código/tests/wiring opt-in; cifras 2111/2100/904 son históricas; etapas 3–5 y el ciclo remoto completo de etapa 6 faltan | `src/atlas/mcp/candidate_triage.py`, `src/atlas/mcp/candidate_stage2.py`, `src/atlas/mcp/candidate_stage2_cursor.py`, `src/atlas/core/orchestrator_parts/maintenance_facade.py` | scan-antes-de-aprobar; ninguna clasificación clean concede ejecución | Completar por work order sin relajar HITL; ADR-076 C sigue rechazado |
| ADR-076 — descubrimiento continuo | Capability/Governance | ACTIVO parcial | A `ACCEPTED/CODE_PRESENT/OPT_IN`; B `ACCEPTED/CODE_PRESENT/OPT_IN`; C `REJECTED/NOT_IMPLEMENTED/ABSENT` | `docs/decisions/adr/adr_076_mcp_continuous_discovery_and_governed_auto_adopt.md`, `docs/canon/decision_registry.jsonl` | Automatiza reseed/vetting acotado, nunca auto-adopción ejecutable | Mantener C ausente y medir A/B antes de promoción runtime |
| ADR-077 — Security Council | Capability/Governance | ACTIVO parcial | A/B/D tienen implementación; B opt-in/default-off y configurado en baseline; C (escalada universal `Task.AWAITING_APPROVAL`) falta; high sigue humano-o-deny | `docs/decisions/adr/adr_077_universal_security_council_gate_and_escalation.md`, `docs/canon/decision_registry.jsonl` | Gate/informe/registro antes del Decider, con límites atómicos explícitos | Cerrar Task boundary e identidad humana antes de ampliar la ceremonia |
| Gmail read-only connector (primer conector real) | Capability/Adapter | ACTIVO | ADR-065; `CODE_PRESENT/TESTED/WIRED`: API/CLI importan diferidamente `AuthBroker`, `ConnectorRegistry` y `GmailReadOnlyConnector`; no hubo llamada Gmail live fresca | `src/atlas/fabric/connectors/gmail.py`, `src/atlas/fabric/testing.py`, `tests/test_os_fabric.py` | Fallback autosuficiente read-only; el MCP externo no lo supersede sin decisión | Mantener sin claim live; decidir sustitución solo con evidencia y ADR |
| Visual Orchestrator / Territories (parked) → reencuadre F5/F6 self-construction | Core/Governance | ACTIVO | ADR-066 (parked, aceptado 2026-07-11); ADR-068 (reencuadre, aceptado 2026-07-11 — supersede el framing de ADR-066) | `docs/decisions/adr/adr_066_visual_orchestrator_and_territories_parked.md`, `adr_068_f5_f6_reframed_self_construction_not_verticals.md` | F5/F6 son núcleo de autoconstrucción, no verticales de negocio (ver Mission Layer/Golden Route arriba) | ADR-068 es la lectura vigente; ADR-066 es el parking histórico que motivó el reencuadre |
| Atlas Constitution Authority | Governance | ACTIVO | ADR-067 (aceptado 2026-07-11) | `docs/decisions/adr/adr_067_atlas_constitution_authority.md` | Autoridad constitucional distribuida (ver también ADR-067, cierre de los 3 ZIPs fuente 2026-07-11) | Foundational governance doc |
| Retirada del canal REST legado de Hermes | Absorbed Pattern/External Service | ACTIVO | ADR-070 (aceptado 2026-07-17, decisión bootstrap N2 revisable por el operador) | `src/atlas/hermes/kanban_bridge.py` | HermesRestAdapter (ADR-011) retirado a favor de `atlas-twin` como cliente canónico | Retirada limpia, cerrada |
| Mission Layer v0 | Core | ACTIVO | ADR-069; `CODE_PRESENT/TESTED/WIRED`; el conteo de 237 misiones fue una observación histórica, no liveness de la candidata | `docs/decisions/adr/adr_069_mission_layer_v0_foundry.md`, `docs/design/mission_layer_self_construction_spec.md`, `src/atlas/api/missions.py`, `schemas/mission.schema.json`, `schemas/mission_receipt.schema.json`, `schemas/soul_manifest.schema.json` | Proyección Mission acotada; el bridge contiene además mutaciones no autorizadas por ADR-058/071 | Mantener proyección pura y resolver `ADC-WO-107` para el boundary mutante |
| Atlas API bridge 7341 | Capability/Adapter | CONTRADICTED | Código/tests/wiring presentes; ADR-058/071 fijan read-only, pero `server.py` expone POST mutantes, incluido approve vía CLI, sin ADR supersedente | `src/atlas/api/server.py`, ADR-058, ADR-071, `ADC-WO-107` | Superficie real con autoridad contractual no reconciliada | Operador elige autorizar un boundary mutante gobernado o restaurar read-only |
| GoldenRoute | Core/Governance | ACTIVO | ADR-069 (closed 2026-07-15); commit `8ec49c3f`; `tests/acceptance/test_self_construction_golden_route.py` xfail retired (green); `tests/test_golden_route.py` | `src/atlas/missions/golden_route.py`, ADR-069 | Public self-construction surface: request(text) → deterministic plan (v0 doc-only append) → unified patch → isolated worktree → observable validation → human approval recorded in Merkle BEFORE acting → apply/commit → receipt | Keep the approval-before-action invariant; widen plan types beyond doc-only append only via a new ADR |
| Self-Build Radar | Governance | ACTIVO | ADR-069; cuatro detectores deterministas con tests; los loops y conteos citados son observaciones históricas | `src/atlas/api/missions.py` (`GET /missions/radar`), ADR-069, `tests/test_os_missions.py` | Capa signal-only sobre Mission data; nunca actúa | Extender solo con evidencia fresca de loops |
| Actor roles + delegation policy | Governance | ACTIVO | Operator policy 2026-07-15 ("Fable solo criterio, Sonnet implementa, Haiku lo mecánico"); memory `feedback-delegate-to-cheaper-models.md` | `docs/design/actor_roles.md`, `AGENTS.md` (authority model), `MEMORY.md` | Defines who Fable/Sonnet/Haiku/Opus are (session drivers, not Atlas components) and the succession protocol for any cold-start model | Keep current as models rotate; any new session driver follows `AGENTS.md` + this doc, no predecessor knowledge assumed |
| Atlas IDE — Void fork (`atlas-ide`) | Absorbed Pattern/Adapter | ACCEPTED_DESIGN / PORT_SOURCE | El fork y su forward-port contienen provider roles, bridge 7342, lifecycle supervision y tests reales; el upstream Void está congelado y no será el host actualizado por sí solo | ADR-078, `docs/canon/product_lineage_registry.jsonl`, `docs/design/ui/void_fork_ux_fusion_inventory_2026-07-18.md` | Donante de capacidades para Atlas Engineering Workbench sobre el host CodeOSS/VSCodium; no autoridad ni producto separado | `ADC-WO-109`: portar progresivamente código útil con procedencia/licencia, puentes Atlas y estrategia upstream; no claim wired en Cut 0 |
| Atlas IDE — Zed fork (`atlas-editor-zed`), referencia/ACP | Absorbed Pattern | ACCEPTED_DESIGN / PATTERN_DONOR | Checkout upstream sin commits Atlas; ACP y patrones de interacción/rendimiento son útiles, con boundary de licencia Apache/GPL explícito | ADR-078, `docs/canon/product_lineage_registry.jsonl`, `src/atlas/acp/server.py`, `docs/design/ui/void_fork_ux_fusion_inventory_2026-07-18.md` | Donante ACP/de patrones e interoperability target; no host, no trasplante completo y no producto aparcado | `ADC-WO-110`: seleccionar comportamiento medible, validar ACP y licencia, y asimilar mediante contrato o componente aislado |

## Absorbed Patterns Matrix: Aider/Cursor/Codex/Claude Code

This matrix details architectural patterns observed in Aider, Cursor, Codex, and Claude Code, prioritized by detected real pressure in Atlas delegations as of 2026-07-08. The goal is selective assimilation of core architectural strengths, not cosmetic features.

| Capability | Aider (Pattern, Reference, Priority) | Cursor (Pattern, Reference, Priority) | Codex (Pattern, Reference, Priority) | Claude Code (Pattern, Reference, Priority) |
|---|---|---|---|---|
| **1. Code Generation/Completion** | *Interactive Diff-based Editing*. Ref: Aider CLI, `git diff` integration. Priority: High. | *Multi-file Contextual Generation*. Ref: Cursor IDE, multi-file chat. Priority: High. | *Function/Block Completion*. Ref: Original Codex papers/demos. Priority: Medium. | *Natural Language to Code (API-driven)*. Ref: Claude API docs, safety features. Priority: Medium. |
| **2. Context Management/Retrieval** | *Git-aware Context Window*. Ref: Aider's `git diff` and `git add -p`. Priority: High. | *Semantic Codebase Indexing*. Ref: Cursor's "smart" context, embeddings/ASTs. Priority: High. | *Prompt Engineering for Context*. Ref: Early LLM usage patterns. Priority: Low. | *Conversation History + File Snippets*. Ref: Claude chat, file attachments. Priority: Medium. |
| **3. Tool Use/Orchestration** | *Shell Command Integration*. Ref: Aider's interactive shell. Priority: High. | *Integrated Terminal & Debugger*. Ref: Cursor IDE features. Priority: High. | *Limited External Tooling*. Ref: Original Codex usage. Priority: Low. | *Function Calling/Tool Use API*. Ref: Claude's tool use capabilities. Priority: High. |
| **4. Self-Correction/Feedback Loops** | *Test-Driven Iteration*. Ref: Aider's `TDD` mode. Priority: High. | *User-Guided Refinement*. Ref: Cursor's chat interface. Priority: Medium. | *Implicit Feedback from User*. Ref: Early LLM interaction. Priority: Low. | *Error Analysis & Suggestion*. Ref: Claude's debugging capabilities. Priority: High. |
| **5. Human-in-the-Loop/Approval** | *Interactive Diff Approval*. Ref: Aider's `git add -p` workflow. Priority: High. | *Chat-based Review & Edit*. Ref: Cursor's integrated chat and editing. Priority: High. | *Manual Review & Copy-Paste*. Ref: Basic LLM interaction. Priority: Low. | *Explicit Approval Prompts*. Ref: Claude's safety features, conversational flow. Priority: High. |

## Zero-Importer Triage Snapshot

Classification refreshed 2026-07-23 (audit pass) against real `grep` evidence
of `src/atlas/` importers, replacing the stale 2026-07-07 snapshot that had
drifted out of sync with `maintenance_facade.py` wiring done on 2026-07-08.
This is not deletion approval; it prevents zero-importer modules from being
called live by accident.

| Module group | State | Rationale | Next action |
| --- | --- | --- | --- |
| `_crawl4ai_worker.py` | KEEP | Subprocess entrypoint used by `CrawlerTool` in isolated venv | Keep out of normal import graph |
| `root_cause_classifier.py`, `benchmark_gate.py`, `failure_lesson_sink.py`, `evolution_gate.py` | KEEP | Injectable components used by ColdUpdate/Batcher/SelfBuild paths when configured | Do not claim always-on; wire only through explicit runtime config |
| `preflight_gate.py`, `batch_premortem.py`, `topic_expander.py`, `panorama_scout.py` | ACTIVO | Imports/callers demuestran `CODE_PRESENT/WIRED`; flags self-build/research observados acreditan configuración, no ejecución live de cada tick | Mantener opt-in y medir una corrida antes de `LIVE_VERIFIED` |
| `lesson_runner.py` | PARK | Las referencias actuales son comentarios/contratos; la auditoría estructural no encuentra caller productivo | Reabrir solo con owner runtime y test de integración |
| `incremental_coder.py`, `sota_snapshot.py` | PARK | Tested workflows, zero non-test callers confirmed 2026-07-23 | Reopen with CLI/runtime owner or quarantine later |
| `history_compactor.py`, `token_budget.py` | PARK | Standalone context utilities, zero non-test callers confirmed 2026-07-23; caller-owned | Wire only when a caller needs them |
| `immunity/live_loop.py` | ACTIVO parcial | `CODE_PRESENT/TESTED/WIRED` por `enable_gate_d_pipeline`; Gate D configurado no demuestra escalada live | Mantener detrás de Gate D y judge fail-closed |
| `mcp/adapter_registry.py` | PARK | `AdapterRegistry`/`computer_control_mcp_adapter` — 0 non-test callers confirmed 2026-07-23 audit (backlog `t3-3-harness-adapter-contract-registry` marked `done`, but nothing in `src/atlas/` imports or validates it; only a comment reference in `desktop_planner.py`) | Wire only when a real caller consumes the registry, e.g. a future planner that validates adapters before dispatch |
| `business/legacy.py` | PARK | `LegacyLinkLayer` (Business Core Fase 15, ADR-061) — 0 non-test callers confirmed 2026-07-23 (`scripts/sanitation_audit.py` direct run); Business Core is draft-first, no real flow consumes it yet | Wire when a real Business Core flow needs legacy-system linking; keep sync off by default per its own docstring |
| `events/core_bridge.py` | PARK | `CoreEventBridge` (ADR-058) — projects `contracts.Event` (real bus) into the OS event canon (`OsEvent`); 0 non-test callers confirmed 2026-07-23 — nothing subscribes it in production, Mission Layer/Self-Build Radar read the real EventBus directly instead | Wire only if a consumer needs the OS canon shape specifically, not the raw bus |
| `fabric/connectors/gmail.py` + auth/registry | VER FILA CANÓNICA | La fila “Gmail read-only connector” anterior contiene la disposición vigente; esta entrada se conserva solo para evitar que un snapshot histórico parezca una segunda autoridad | No duplicar estado |
| `security/node_identity.py` | KEEP by design | Backlog `t6-node-identity-module` (`done`): standalone Ed25519 keypair/heartbeat module, explicitly documented as consumer-less until a second real node exists (Hermes VPS off since May) | Wire only when a real second node exists; not a gap |

## Operating Rule

New work must add or update one row here before adding another roadmap item.
If a capability has no non-test consumer, mark it honestly as `PENDIENTE`,
`PARK`, `KEEP as entrypoint/injectable`, or `VAPOR`; do not call it live.
