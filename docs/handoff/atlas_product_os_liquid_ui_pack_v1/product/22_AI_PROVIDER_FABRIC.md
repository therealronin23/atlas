# AI Provider Fabric

AI providers are integrations with policy, not magic models.

Each provider/model declares:

- modality capabilities;
- tool capabilities;
- context limits;
- cost;
- latency;
- data retention assumptions;
- allowed data classes;
- local/cloud boundary;
- fallback policy;
- audit requirements.

No cloud model for sensitive data without explicit approval.
