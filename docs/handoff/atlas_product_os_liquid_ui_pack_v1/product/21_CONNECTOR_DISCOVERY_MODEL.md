# Connector Discovery Model

Atlas must discover how a platform can be connected.

It checks for official APIs, OAuth, OpenAPI, webhooks, MCP, SDKs, CLI, database, files, browser app, local desktop app and manual export.

Discovery output is a connection route decision with risk, capabilities and fallback plan.
