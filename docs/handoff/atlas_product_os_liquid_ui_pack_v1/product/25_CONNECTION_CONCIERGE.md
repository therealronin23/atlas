# Connection Concierge

Connection Concierge guides setup in plain language.

It detects platform, recommends route, opens the right official page, guides credential creation if manual, validates credentials, tests the connection, explains permissions and creates a safe connector profile.

The user should not need to search docs for OAuth scopes, redirect URIs, API endpoints or webhook secrets unless unavoidable.
