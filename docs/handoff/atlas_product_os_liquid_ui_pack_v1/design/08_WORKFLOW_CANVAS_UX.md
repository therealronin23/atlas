# Workflow Canvas UX

Canvas with node palette, center graph, right inspector and bottom execution timeline. Modes: edit, simulate, execute, review.

Simulation is dotted and non-external. Execution checks capabilities and Gates.
