# UI State System

Global states: empty, loading, ready, dirty, validating, needs_review, waiting_approval, blocked, executing, completed, failed, offline, degraded, lockdown.

Every state has text, icon, color/pattern, primary action, secondary action and event.
