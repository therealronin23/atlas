# AI Provider Connection Study

AI provider setup must be guided.

Claude/OpenAI/Gemini/API-compatible providers may need API keys or OAuth depending on provider. Atlas must guide manual key creation when necessary, validate, store references securely, set cost/privacy policies and define allowed data classes.
