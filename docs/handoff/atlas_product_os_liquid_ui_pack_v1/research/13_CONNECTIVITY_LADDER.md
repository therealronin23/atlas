# Connectivity Ladder

For every system, Atlas chooses the safest route available.

API-first. Computer-use last. Manual fallback accepted when automation is forbidden, unreliable or too risky.

Each route declares auth, capabilities, data classes, gates, audit, health checks and tests.
