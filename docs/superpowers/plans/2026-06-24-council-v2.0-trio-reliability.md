# Cónclave v2.0 — Fiabilidad del trío — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Subir el trío de revisores de ~1/3 a ≥2/3 voces útiles en vivo, vía tres cambios quirúrgicos (config Gemini + reintento transitorios + parseo Kimi), sin tocar el camino one-shot de `verified_producer`.

**Architecture:** Fix 0 corrige el modelo de Gemini en `DEFAULT_PROVIDERS` (config, raíz del 503 crónico). Fix 1 añade reintento ante transitorios en `InferenceHub._call_provider_real` (beneficia a todo consumidor del hub). Fix 2 hace robusto el parseo de severidad en `LlmReviewer.review` (anclado a 1ª línea, sin perder contenido). Los tres son independientes y se pueden implementar/commitear por separado.

**Tech Stack:** Python 3, litellm (mockeado en tests vía `monkeypatch.setattr`), pytest, mypy strict.

## Global Constraints

- Tests deben cubrir el cambio antes de merge (AGENTS.md Regla 7).
- mypy strict verde: `MYPYPATH=src python -m mypy src/atlas/` (AGENTS.md).
- Cero deps nuevas (AGENTS.md Regla 6) — solo stdlib (`time`, `random`) + litellm ya presente.
- `WORK_LEDGER.md` actualizado en el MISMO commit que el trabajo (parte de "done").
- Antes de editar, el diff intencionado está declarado en el spec (AGENTS.md Regla 8).
- Comandos de test: `cd ~/proyectos/atlas-core && source .venv/bin/activate && PYTHONPATH=src python -m pytest ...`
- Fail-closed se preserva: una objeción ilegible nunca se trata como "sin objeción" (sigue siendo MAJOR).
- Identifiers verificados en vivo 2026-06-24: `gemini/gemini-2.5-flash` responde estable; nombres de excepción litellm: `ServiceUnavailableError`, `InternalServerError`, `Timeout`, `APIConnectionError`.

---

### Task 1: Fix 0 — Modelo de Gemini a `gemini-2.5-flash` (config)

**Files:**
- Modify: `src/atlas/core/inference_hub.py:175-179` (provider `gemini_free`)
- Test: `tests/test_inference_hub_real.py:28-33` (`test_default_gemini_provider_uses_stable_model_id`)

**Interfaces:**
- Consumes: nada.
- Produces: `DEFAULT_PROVIDERS` con `gemini_free.model_id == "gemini-2.5-flash"` y `litellm_model == "gemini/gemini-2.5-flash"`.

- [ ] **Step 1: Actualizar el test existente al modelo nuevo**

En `tests/test_inference_hub_real.py`, reemplazar el cuerpo de `test_default_gemini_provider_uses_stable_model_id`:

```python
def test_default_gemini_provider_uses_stable_model_id() -> None:
    gemini = next(p for p in DEFAULT_PROVIDERS if p.name == "gemini_free")
    # gemini-3.5-flash da 503 crónico (free saturado, verificado en vivo 2026-06-24);
    # 2.5-flash responde estable. Pin, NO alias -latest (también 503).
    assert gemini.model_id == "gemini-2.5-flash"
    assert gemini.litellm_model == "gemini/gemini-2.5-flash"
    assert not gemini.model_id.endswith("-latest")
```

- [ ] **Step 2: Correr el test para verificar que FALLA**

Run: `PYTHONPATH=src python -m pytest tests/test_inference_hub_real.py::test_default_gemini_provider_uses_stable_model_id -v`
Expected: FAIL — `assert 'gemini-3.5-flash' == 'gemini-2.5-flash'`

- [ ] **Step 3: Cambiar el modelo en el provider**

En `src/atlas/core/inference_hub.py`, provider `gemini_free` (~línea 178-179), cambiar:

```python
        model_id="gemini-2.5-flash",
        litellm_model="gemini/gemini-2.5-flash",
```

- [ ] **Step 4: Correr el test para verificar que PASA**

Run: `PYTHONPATH=src python -m pytest tests/test_inference_hub_real.py::test_default_gemini_provider_uses_stable_model_id -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/atlas/core/inference_hub.py tests/test_inference_hub_real.py
git commit -m "fix(inference): gemini_free a gemini-2.5-flash (3.5-flash 503 crónico)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Fix 1 — Reintento ante transitorios en `InferenceHub`

**Files:**
- Modify: `src/atlas/core/inference_hub.py` (imports, constantes de módulo, `__init__`, `_call_provider_real`)
- Test: `tests/test_inference_hub_real.py` (nueva clase `TestRetry`)

**Interfaces:**
- Consumes: `InferenceHub(providers=..., mode="live")`, `litellm.completion` (mockeable).
- Produces:
  - Constantes de módulo: `INFER_MAX_RETRIES: int = 2`, `INFER_RETRY_BASE_S: float = 0.5`.
  - Helper de módulo: `_is_transient(exc: BaseException) -> bool`.
  - `InferenceHub.__init__(..., sleep_fn: Callable[[float], None] = time.sleep)` — sleep inyectable.

- [ ] **Step 1: Escribir el test que falla — transitorio reintenta y al 2º intento tiene éxito**

Añadir al final de `tests/test_inference_hub_real.py`:

```python
class TestRetry:
    def test_transient_503_retries_then_succeeds(
        self, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        providers = [_providers_with_keys(monkeypatch)[0]]  # solo groq_test, L1
        calls = {"n": 0}

        def flaky_completion(**kwargs: Any) -> Any:
            calls["n"] += 1
            if calls["n"] == 1:
                raise litellm.exceptions.ServiceUnavailableError(
                    message="503 high demand", model="m", llm_provider="p"
                )
            return _ok_completion(text="al fin")

        sleeps: list[float] = []
        monkeypatch.setattr(litellm, "completion", flaky_completion)
        hub = InferenceHub(
            providers=providers, mode="live", sleep_fn=sleeps.append
        )
        resp = hub.infer(InferenceRequest(prompt="hola", level=InferenceLevel.L1))

        assert resp.success is True
        assert resp.text == "al fin"
        assert calls["n"] == 2          # reintentó exactamente una vez
        assert len(sleeps) == 1         # durmió entre intentos (mockeado, instantáneo)

    def test_permanent_404_does_not_retry(
        self, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        providers = [_providers_with_keys(monkeypatch)[0]]
        calls = {"n": 0}

        def not_found(**kwargs: Any) -> Any:
            calls["n"] += 1
            raise litellm.exceptions.NotFoundError(
                message="model not found", model="m", llm_provider="p"
            )

        monkeypatch.setattr(litellm, "completion", not_found)
        hub = InferenceHub(
            providers=providers, mode="live", sleep_fn=lambda _s: None
        )
        resp = hub.infer(InferenceRequest(prompt="hola", level=InferenceLevel.L1))

        assert resp.success is False
        assert calls["n"] == 1          # 404 = config, NO se reintenta

    def test_transient_exhausts_retries_then_fails(
        self, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        providers = [_providers_with_keys(monkeypatch)[0]]
        calls = {"n": 0}

        def always_503(**kwargs: Any) -> Any:
            calls["n"] += 1
            raise litellm.exceptions.ServiceUnavailableError(
                message="503", model="m", llm_provider="p"
            )

        monkeypatch.setattr(litellm, "completion", always_503)
        hub = InferenceHub(
            providers=providers, mode="live", sleep_fn=lambda _s: None
        )
        resp = hub.infer(InferenceRequest(prompt="hola", level=InferenceLevel.L1))

        assert resp.success is False
        assert calls["n"] == 3          # 1 inicial + INFER_MAX_RETRIES (2)
```

- [ ] **Step 2: Correr los tests para verificar que FALLAN**

Run: `PYTHONPATH=src python -m pytest tests/test_inference_hub_real.py::TestRetry -v`
Expected: FAIL — `TypeError: __init__() got an unexpected keyword argument 'sleep_fn'`

- [ ] **Step 3: Añadir imports y constantes de módulo**

En `src/atlas/core/inference_hub.py`, cabecera: asegurar `import time`, `import random`, y `from collections.abc import Callable` (o `from typing import Callable` si el resto del archivo lo usa así — seguir el estilo existente del archivo). Cerca de `RATE_LIMIT_COOLDOWN_S` añadir:

```python
# Reintento ante transitorios (503/500/timeout/conexión). Constantes de módulo
# diseñadas-para-promover: el día que un Provider concreto necesite otra política
# (medido), se promueven a campo de Provider con estas como default — cambio aditivo.
INFER_MAX_RETRIES = 2          # reintentos extra → 3 intentos totales
INFER_RETRY_BASE_S = 0.5       # backoff: intento k espera ~BASE*2**(k-1) + jitter

# Substring del nombre de excepción litellm que SÍ se reintenta. RateLimit queda
# fuera (ya tiene cooldown); Authentication/BadRequest/NotFound son permanentes.
_TRANSIENT_MARKERS = ("ServiceUnavailable", "InternalServer", "Timeout", "APIConnection")


def _is_transient(exc: BaseException) -> bool:
    name = type(exc).__name__
    return any(m in name for m in _TRANSIENT_MARKERS)
```

- [ ] **Step 4: Añadir `sleep_fn` al `__init__`**

En `InferenceHub.__init__` (línea 258-272), añadir el parámetro y guardarlo:

```python
    def __init__(
        self,
        providers: list[Provider] | None = None,
        mode: str = "auto",
        merkle: "MerkleLogger" | None = None,
        transparency: "TransparencyGateway | None" = None,
        sleep_fn: Callable[[float], None] = time.sleep,
    ) -> None:
        if mode not in ("auto", "live", "stub"):
            raise ValueError(f"mode invalido: {mode}")
        self._providers = providers or DEFAULT_PROVIDERS
        self._mode = os.environ.get("ATLAS_INFERENCE_MODE", mode)
        self._calls: list[dict[str, Any]] = []
        self._rate_limited_until: dict[str, float] = {}
        self._merkle = merkle
        self._transparency = transparency
        self._sleep = sleep_fn
```

- [ ] **Step 5: Envolver la llamada litellm con el bucle de reintento**

En `_call_provider_real`, reemplazar el bloque `try/except` que rodea `litellm.completion` (líneas ~504-551). El `completion = litellm.completion(...)` pasa a vivir dentro de un bucle; el `except` decide reintentar (transitorio, quedan intentos) o caer al manejo de fallo existente. La construcción de `extra_kwargs`/`messages` queda IGUAL, antes del bucle. Estructura nueva:

```python
        last_exc: BaseException | None = None
        for attempt in range(INFER_MAX_RETRIES + 1):
            try:
                assert litellm is not None
                completion = litellm.completion(
                    model=provider.litellm_model,
                    messages=messages,
                    max_tokens=request.max_tokens,
                    temperature=request.temperature,
                    **extra_kwargs,
                )
                last_exc = None
                break
            except Exception as exc:  # noqa: BLE001 — clasificamos abajo
                last_exc = exc
                if _is_transient(exc) and attempt < INFER_MAX_RETRIES:
                    backoff = INFER_RETRY_BASE_S * (2 ** attempt)
                    self._sleep(backoff + random.uniform(0.0, INFER_RETRY_BASE_S))
                    continue
                break

        if last_exc is not None:
            duration_ms = int((time.perf_counter() - start) * 1000)
            err_name = type(last_exc).__name__
            err_msg = f"{err_name}: {last_exc}"
            self._classify_error(provider, last_exc)
            self._log_model_call(provider, request, success=False, error=err_msg)
            self._calls.append({
                "provider": provider.name,
                "task_id": request.task_id,
                "level": request.level.value,
                "success": False,
                "error": err_msg,
                "mode": "live",
            })
            return InferenceResponse(
                text="", provider=provider.name, model=provider.model_id,
                level=provider.level, latency_ms=duration_ms, success=False,
                error=err_msg, mode="live",
            )
```

Nota: el código que sigue (extracción de texto/tokens, marcar OK, log de éxito, return success) queda IGUAL — solo se ejecuta cuando `last_exc is None` (éxito).

- [ ] **Step 6: Correr los tests de reintento — PASAN**

Run: `PYTHONPATH=src python -m pytest tests/test_inference_hub_real.py::TestRetry -v`
Expected: PASS (3 tests)

- [ ] **Step 7: Correr toda la suite de inference + mypy (no regresión)**

Run: `PYTHONPATH=src python -m pytest tests/test_inference_hub_real.py -q && MYPYPATH=src python -m mypy src/atlas/core/inference_hub.py`
Expected: todos PASS, mypy sin errores nuevos.

- [ ] **Step 8: Commit**

```bash
git add src/atlas/core/inference_hub.py tests/test_inference_hub_real.py
git commit -m "feat(inference): reintento ante transitorios (503/500/timeout/conn)

Allowlist por nombre de excepción; 2 reintentos, backoff exp+jitter acotado,
sleep inyectable. 404/auth/ratelimit NO reintentan. Constantes diseñadas-para-promover.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Fix 2 — Parseo robusto del detalle en `LlmReviewer.review`

**Files:**
- Modify: `src/atlas/core/deliberation_council.py:73-88` (`LlmReviewer.review`)
- Test: `tests/test_deliberation_council.py` (junto a los `test_review_*` existentes)

**Interfaces:**
- Consumes: `_FakeHub` (ya existe en el test), `LlmReviewer(reviewer_id, provider, hub, level)`.
- Produces: `LlmReviewer.review` con detección de severidad anclada a 1ª línea normalizada; sin severidad limpia → `Severity.MAJOR` + texto COMPLETO como detalle.

- [ ] **Step 1: Escribir los tests que fallan**

Añadir en `tests/test_deliberation_council.py` (junto a los `test_review_*`):

```python
def test_review_no_severity_keeps_full_text_as_detail() -> None:
    from atlas.core.deliberation_council import LlmReviewer

    # Kimi devuelve contenido real SIN severidad en 1a línea (bug reproducido en vivo).
    hub = _FakeHub("Esta decisión asume disponibilidad no probada.\ny encima ignora X.")
    r = LlmReviewer("kimi", "moonshot", hub, InferenceLevel.L2)
    obj = r.review("diff", "ctx")
    assert obj.severity is Severity.MAJOR  # fail-closed, sin cambio
    assert "disponibilidad no probada" in obj.detail  # NO se tira lines[0]
    assert "ignora X" in obj.detail


def test_review_bracketed_severity_in_first_line() -> None:
    from atlas.core.deliberation_council import LlmReviewer

    hub = _FakeHub("[MAJOR] rompe el contrato de tipos.")
    r = LlmReviewer("g", "google", hub, InferenceLevel.L1)
    obj = r.review("diff")
    assert obj.severity is Severity.MAJOR
    assert "rompe el contrato" in obj.detail


def test_review_negation_is_not_false_positive() -> None:
    from atlas.core.deliberation_council import LlmReviewer

    # "no es MAJOR" NO debe casar severidad (anclado a 1a línea, no scan global).
    hub = _FakeHub("no es MAJOR, pero hay un caso límite con NONE de los flujos.")
    r = LlmReviewer("m", "mistral", hub, InferenceLevel.L2)
    obj = r.review("diff")
    assert obj.severity is Severity.MAJOR          # default fail-closed
    assert "caso límite" in obj.detail             # texto completo conservado
```

- [ ] **Step 2: Correr los tests para verificar que FALLAN**

Run: `PYTHONPATH=src python -m pytest tests/test_deliberation_council.py -k "full_text or bracketed or negation" -v`
Expected: FAIL — el primero por `detail` vacío/sin `lines[0]`; el bracketed por severidad MAJOR-default con detalle perdido.

- [ ] **Step 3: Reescribir `LlmReviewer.review` con parseo anclado**

En `src/atlas/core/deliberation_council.py`, reemplazar el cuerpo desde el parseo (líneas 85-88) por una detección anclada a 1ª línea normalizada. Justo encima de la clase o como método privado, añadir un helper de normalización; el resto de `review` (la llamada y el fail-closed de `not resp.success`) NO cambia:

```python
        lines = resp.text.strip().splitlines()
        first_norm = lines[0].strip().strip("[](){}*#:.- ").upper() if lines else ""
        if first_norm in _SEVERITIES:
            sev = _SEVERITIES[first_norm]
            detail = "\n".join(lines[1:]).strip()
        else:
            # 1a línea no es severidad limpia: fail-closed MAJOR, pero CONSERVA el
            # texto completo (no tirar lines[0], que es el contenido real de la voz).
            # Anclado a 1a línea a propósito: NO escanear el cuerpo (evita el falso
            # positivo "no es MAJOR").
            sev = Severity.MAJOR
            detail = resp.text.strip()
        return Objection(self._id, self._provider, sev, detail)
```

- [ ] **Step 4: Correr los tests nuevos + los `test_review_*` existentes**

Run: `PYTHONPATH=src python -m pytest tests/test_deliberation_council.py -v`
Expected: PASS — incluidos los 3 nuevos y los existentes (`test_review_parses_severity_and_detail`, `..._unparseable_first_line_is_major_failclosed`, `..._failed_inference_is_failclosed_major`).

Nota de regresión esperada: `test_review_unparseable_first_line_is_major_failclosed` usaba `_FakeHub("bla bla sin etiqueta")` y antes esperaba detalle vacío. Con el fix, ahora el detalle = texto completo. **Actualizar ese test** si su aserción exige detalle vacío:

```python
def test_review_unparseable_first_line_is_major_failclosed() -> None:
    from atlas.core.deliberation_council import LlmReviewer

    hub = _FakeHub("bla bla sin etiqueta")
    r = LlmReviewer("g", "google", hub, InferenceLevel.L1)
    obj = r.review("d")
    assert obj.severity is Severity.MAJOR
    assert obj.detail == "bla bla sin etiqueta"   # conserva contenido (antes: vacío)
```

- [ ] **Step 5: mypy strict del módulo**

Run: `MYPYPATH=src python -m mypy src/atlas/core/deliberation_council.py`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add src/atlas/core/deliberation_council.py tests/test_deliberation_council.py
git commit -m "fix(council): parseo de severidad anclado a 1a línea (conserva detalle)

Si la 1a línea no es severidad limpia: MAJOR fail-closed + texto COMPLETO como
detalle (antes se descartaba lines[0]). Tolera [MAJOR]; evita falso positivo 'no es MAJOR'.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Cierre DoD — suite completa, ledger, CAPABILITIES, smoke vivo

**Files:**
- Modify: `WORK_LEDGER.md` (nodo v2.0 → ✅)
- Modify: `docs/governance/CAPABILITIES.md` (nota honesta de fiabilidad del trío)
- Modify: `docs/design/design_deliberation_council.md` (nota de cierre v2.0, opcional 1 línea)

**Interfaces:**
- Consumes: Tasks 1-3 mergeadas.
- Produces: estado "done" registrado en las 3 autoridades (ledger=WHERE, CAPABILITIES=honestidad, design=HOW).

- [ ] **Step 1: Suite completa + mypy global verde**

Run: `PYTHONPATH=src python -m pytest tests/ -q && MYPYPATH=src python -m mypy src/atlas/`
Expected: suite verde, mypy strict sin errores nuevos. (Anotar el conteo real; no inventar.)

- [ ] **Step 2: Smoke en vivo del trío (árbitro real de la DoD)**

Run:
```bash
cd ~/proyectos/atlas-core && set -a && source .env && set +a && source .venv/bin/activate && PYTHONPATH=src python - <<'PY'
from atlas.core.deliberation_council import build_trio_reviewers
reviewers = build_trio_reviewers()
useful = 0
for r in reviewers:
    o = r.review("Decisión de prueba: ¿migrar X a Y?", "contexto mínimo")
    ok = bool((o.detail or "").strip()) and o.detail != "revisión no disponible (fail-closed)"
    print(r.reviewer_id, o.severity.name, "detalle_len=", len(o.detail or ""), "útil=", ok)
    useful += ok
print("VOCES ÚTILES:", useful, "/ 3")
PY
```
Expected: **≥2/3 voces útiles** (Gemini ya con 2.5-flash responde; Kimi conserva detalle). Si sale <2/3, NO declarar la DoD cumplida — investigar (es el criterio honesto, `wire-before-claim`).

- [ ] **Step 3: Actualizar WORK_LEDGER.md**

En el nodo `v2.0 fiabilidad del trío`, marcar ✅ con una línea de evidencia: "Fix 0 (gemini-2.5-flash) + Fix 1 (reintento transitorios) + Fix 2 (parseo anclado). Smoke vivo: N/3 voces útiles." (N = el número real del Step 2.)

- [ ] **Step 4: Nota honesta en CAPABILITIES.md**

Añadir/actualizar la entrada del Cónclave: la fiabilidad del trío pasó de ~1/3 a N/3 (con el número del smoke real). El reintento cubre transitorios; el fallo correlacionado NIM (Kimi+Mistral misma infra) queda como límite conocido → v2.0.5.

- [ ] **Step 5: Commit de cierre**

```bash
git add WORK_LEDGER.md docs/governance/CAPABILITIES.md docs/design/design_deliberation_council.md
git commit -m "docs(council): cierra v2.0 fiabilidad del trío (ledger + CAPABILITIES + smoke)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Notas de cierre

- Tras Task 4, la rama `feat/council-v2.0-trio-reliability` queda lista para merge/PR (decidir con el usuario; no auto-push).
- Follow-up registrado: v2.0.5 (fallback de slot por-linaje, cura el fallo correlacionado NIM) + investigación de proveedores gratis/multi-cuenta por linaje. NO empezar sin hechos (proveedores vivos verificados).
